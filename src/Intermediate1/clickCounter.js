import React, { Component } from 'react'

class clickCounter extends Component{
    render(){
        return(
            <button>
                click several times
            </button>
        )
    }
}
export default clickCounter